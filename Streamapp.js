import {userId, token} from './Login/Signin';

<StreamApp
    apiKey="{z5yxt6suut2f}"
    appId="{70520}"
    userId="{USER_ID}"
    token="{TOKEN}"
    analyticsToken="{ANALYTICS_TOKEN}"
>
    {/* everything from your application interacting with Stream should be nested here */}
</StreamApp>